name: Upload Artifact

on: push

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4.1.1

            - name: ls
              run : ls -lah
              
            - name: artifact
              uses: actions/upload-artifact@v4
              with:
                name: my-artifact
                path: .github/workfows/*



